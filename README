Help for these IDL function graphics routines can be obtained by running
FG_HELP
from the IDL prompt. This should open html pages in a browser window.

In order to generate the help in the first place, Mike Galloy's IDLDOC (github.com/mgalloy/idldoc) is required. Documentation should be ouput to a subfolder to the code folder named "doc/".